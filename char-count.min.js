/*!charCount v1.0 | (c) @ashiqulshourav */
Element.prototype.characterCounting = function(e) {
    var t, n, s, l, o = (e = e || {}).maxChar || 50,
        r = e.buttonHideText || "Show Less",
        i = e.buttonShowText || "Show More";
    let c = [],
        a = this.querySelectorAll("p");
    a.forEach((e, t) => { c.push(e.innerText.length) });
    let h, d, u = function e(t) {
            let n = 0;
            for (let s = 0; s < t.length; s++)
                if ((n += t[s]) >= o) return s
        }(c),
        p = (t = c, n = u, 0 === n ? o : d = 1 === n ? o - t[0] : o - (total = (h = t.slice(0, n)).reduce((e, t) => e + t, 0))),
        f, g, T, L;
    s = a, l = u, f = s[l].innerHTML, g = f.slice(0, p), T = f.slice(p, s[l].innerHTML.length), s[l].innerHTML = `${g}<span class="end-dots">...</span>`, L = document.createElement("button"), L.classList.add("content-toggle-btn"), L.innerText = i, s[l].parentNode.appendChild(L), s[l].parentNode.querySelector("button").addEventListener("click", function(e) { if (e.preventDefault(), this.classList.toggle("less-content"), this.classList.contains("less-content")) { this.innerText = r, a[l].querySelector(".end-dots").remove, s[l].innerHTML = `${g}${T}`; for (let t = l + 1; t < c.length; t++) a[t].setAttribute("style", " block") } else { this.innerText = i, s[l].innerHTML = `${g}<span class="end-dots">...</span>`; for (let n = l + 1; n < c.length; n++) a[n].style.display = "none" } });
    for (let y = u + 1; y < c.length; y++) a[y].style.display = "none"
};